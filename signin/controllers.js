function SignInCtrl($scope, $http) {
    //$scope.feeds = Feed.query();
}

angular.module('app').controllerProvider.register('SignInCtrl', ['$scope', 'authentication', function ($scope, authentication) {
}]);